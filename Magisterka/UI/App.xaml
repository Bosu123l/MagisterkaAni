<Application x:Class="UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UI"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="Button" x:Key="OpacityMenuButton">
            <Setter Property="Width" Value="50" />
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="#FF575757"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style TargetType="LinearGradientBrush" x:Key="LinearOpacityGradientBrush">
            <Setter Property="EndPoint" Value="0.5,1" />
            <Setter Property="MappingMode" Value="RelativeToBoundingBox"/>
            <Setter Property="StartPoint" Value="0.5,0"/>  
        </Style>
    </Application.Resources>
</Application>
