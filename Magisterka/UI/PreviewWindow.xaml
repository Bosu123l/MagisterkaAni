<Window x:Name="window" x:Class="UI.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Podgląd" Height="500" Width="500">
    <Grid
                Margin="5,5,5,3.584"
                Background="#FFD4D4D4"
                d:LayoutOverrides="LeftMargin, RightMargin">
        <ScrollViewer Cursor="Hand" HorizontalScrollBarVisibility="Visible">
            <Image Source="{Binding PreviweImage, ElementName=window}" />
        </ScrollViewer>
    </Grid>
</Window>